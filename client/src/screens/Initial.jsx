import React from 'react'

const Initial = ({ enrollandregister }) => {
  return (
    <div>
        <button className='btn' onClick={enrollandregister}>Start</button>
    </div>
  )
}

export default Initial